import { UserNote } from 'lib/models/randomStuffModels'

/* export function userNoteReducer(state: UserNotesModel, action: NoteActionTypes) {
  switch (action.type) {
    case 'reload':
      return { ...state, noteTitles: action.payload.noteTitles, selectedNote: undefined, username: action.payload.username }
    case 'add-note':
      return { ...state, selectedNote: { title: '', body: '', dateCreated: '', dateModified: '' } }
    default: {
      throw 'invalid type'
    }
  }
} */
